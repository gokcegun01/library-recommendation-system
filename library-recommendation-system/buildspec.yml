version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd library-recommendation-system
      - npm ci

  build:
    commands:
      - npm run build

artifacts:
  base-directory: library-recommendation-system/dist
  files:
    - '**/*'

